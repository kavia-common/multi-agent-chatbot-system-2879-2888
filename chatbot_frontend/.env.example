# PUBLIC_INTERFACE
# Backend base URL used by the frontend to call the API.
# Leave empty to use relative '/api' which works with the CRA dev proxy and same-origin deployments.
# Example for explicit backend URL (different origin): http://localhost:8000/api
# Example for deployed same-origin: https://your-domain.com/api
REACT_APP_BACKEND_URL=
